/* Create and Convert a function that multiplies two numbers into an arrow function. */
function multiply(a, b) {
    return a * b;
}
//Convert in array function 
var multiplyArray = function (a, b) {
    return a * b;
};
