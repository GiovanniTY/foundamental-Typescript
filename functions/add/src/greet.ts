/* Write a function greet that takes a string name and returns a greeting message.
 The name parameter should have a default value of "Guest". */

 function greet(guess:string):string{
    return 'Hello' + guess;
 }